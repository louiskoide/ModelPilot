# ModelPilot delivery — September 25, 2026

The requested M0 replication is complete: 213 real API requests across Haiku 4.5, Sonnet 5 and Opus 5, $3.2075768 calculated cost, 611.547 seconds. All returned model IDs match the requests. No additional API spending was needed for delivery. See [results](m0-replication-results.md). Raw API observations remain in the ignored local runs directory and are not published.

Switches wrote cache; prior settings usually reused it; every 330-second check rewrote. Opus sometimes missed earlier, including a 240-second probe, so warm-target status alone must not authorize a cost-motivated switch. Reserve conservatively for writes and reconcile actual usage. The return-model order did not test returning to Opus; adaptive thinking was disabled. Those limits remain explicit rather than being presented as proven behavior.

Also included: experimental Jev adapter, ModelPilot observer adapter, conservative cost/action preparation, fixture-only atomic dispatch, loopback streaming/cancellation tests, and the proxy accounting-log shutdown fix. Full offline regression: 292 tests passed in 68.633 seconds on September 24. These changes do not enable active paid ModelPilot routing or establish savings. See [adapter progress and remaining gates](bench-adapters.md).

This delivery branches from GitHub main at 60c8ddf. Newer independent Jev benchmark work exists on m6-plan (b7dea13); reconcile the two implementations before merging them together. No final task specs or split hashes were changed.

Remaining beyond the screenshot's cache-measurement scope: real-client action dispatch, worker/reference-output policy integration, provider compatibility validation, router pricing, and active-arm benchmark eligibility. The 40-task comparative benchmark has not been completed.
